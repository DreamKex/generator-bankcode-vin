version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: generator-bankcode-vin
    environment:
      - CARD_NO_LEN=${CARD_NO_LEN:-19}
      - TOAST_DURATION=${TOAST_DURATION:-3}
    ports:
      - "8000:8000"
    restart: unless-stopped
